import React from "react";
import { Pool } from "./Pool";
export const AddressCell = ({ data, direction, customCells, }) => {
    return (<>
      <p>Working</p>
      <Pool cellData={data} direction={direction} customCells={customCells}/>
    </>);
};
